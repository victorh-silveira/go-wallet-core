**Contrato (futuro)**

Testes de contrato estaveis (ex.: schema OpenAPI vs respostas reais) podem viver nesta pasta.

Para este servico, o contrato HTTP e coberto por testes de handler em `tests/unit/handler` e pela especificacao em `api/swagger.yaml`.
