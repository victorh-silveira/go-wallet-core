**E2E (futuro)**

Testes de ponta a ponta com processo real ou binario compilado podem ser adicionados aqui (ex.: subir servidor, `httptest` externo, ou `go test` com `testscript`).

Hoje a cobertura de HTTP e fluxo de negocio esta em `tests/unit/handler` e `tests/integration`.
