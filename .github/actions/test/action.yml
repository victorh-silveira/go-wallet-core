name: Testes Go
description: Go 1.26 — go test em todo o modulo

runs:
  using: composite

  steps:
    - name: Configurar Go
      uses: actions/setup-go@v5
      with:
        go-version: "1.26"
        cache: true

    - name: Executar testes
      shell: bash
      run: go test ./... -count=1
